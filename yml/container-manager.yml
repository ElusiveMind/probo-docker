hostname: localhost
port: 3020

instanceName: '$CM_INSTANCE_NAME'
api:
  url: 'http://localhost:3012'

auth:
  url: 'http://localhost:3012/auth_lookup'

loom:
  url: 'http://localhost:3060'
  token: $PROBO_LOOM_SERVER_TOKEN

assets:
  url: "$ASSET_SERVER_URL"
  token: $PROBO_ASSET_SERVER_TOKEN

buildUrl: '$PROBO_BUILD_URL'
endpointUrl: '$DRUPAL_ENDPOINT_URL'
